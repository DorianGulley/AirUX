{
  "name": "airux",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "check": "biome check . && pnpm worker:types:check && pnpm typecheck && pnpm test",
    "db:lint": "supabase db lint --local --level error --fail-on error",
    "db:migration:new": "supabase migration new",
    "db:reset": "supabase db reset --local",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:test": "supabase test db --local supabase/tests/database",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests",
    "typecheck": "tsc --build",
    "worker:deploy": "pnpm --filter @airux/worker run deploy",
    "worker:dev": "pnpm --filter @airux/worker run dev",
    "worker:types": "pnpm --filter @airux/worker run types",
    "worker:types:check": "pnpm --filter @airux/worker run types:check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "supabase": "2.113.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.120.0"
  }
}
